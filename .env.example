# Public address of the deployed Registry contract on GenLayer Bradbury.
# No PRIVATE_KEY here — this project has no server-side wallet. Every Deal
# contract is deployed and funded directly from the buyer's own wallet.
REGISTRY_ADDRESS=
NATIVE_CURRENCY_NAME=GEN
NATIVE_CURRENCY_SYMBOL=GEN
PORT=3000
